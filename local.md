# MissionInsights.ai — Local Working Notes

> Local-only scratch/reference doc (tracked, but working notes). Updated 2026-07-01.
> Paste this whole file into a new session to bring the assistant up to speed.
> ⚠ **The app was renamed Atlas → MissionInsights.ai** (display name only, commit `e95dd52`).
>   The `X-Atlas-Updated-At` / `X-Atlas-Base-Updated-At` server headers and the
>   `buildAtlasComputed`/`D.atlasComputed` identifiers are UNCHANGED (server contract) —
>   do NOT rename those. Function names in this doc that say "ATLAS save()" still mean the app's `save()`.
> Covers the **Executive Home** dashboard (new), Contract Management, the GovWin capture pipeline,
> Proposals, Strategy (now with **data-bound Goals & KPIs**), the **Commercial** module, the
> **Customers** registry, the **Valuation** page, **Prospects** (BD accounts/contacts), and
> **access-control roles** (Capture + the new **`exec`** role).
> `PROJECT_HANDOFF.md` is the older committed handoff and predates most of this.
> **Newest-first change log is at the bottom** — read it for the full detail of recent work.

## New-session quick start
| Thing | Value |
|---|---|
| Working dir (reliable) | `C:\Users\saqib\OneDrive - CommunityForce Inc\Documents\CFResourcePlanner` — the **OneDrive-redirected** path. The plain `C:\Users\saqib\Documents\CFResourcePlanner` is a junction that can stop resolving mid-session. Prefer the OneDrive path; quote it (spaces). |
| The app | **MissionInsights.ai** (formerly Atlas) — single-file SPA: `index.html` (HTML+CSS+JS, global `D`); Express `server.js` serving it + APIs; Railway Postgres. Top menu is now a **light/white header** (navy gradient removed, commit `86b3c9f`). |
| Live URL | https://cfresourceplanner-production.up.railway.app |
| GitHub | `igrant9679/CFResourcePlanner`, branch `main` (push → Railway auto-deploys ~1–3 min) |
| Login (admin) | `khaja@communityforce.com` / `Namtra_CF27` |
| Preview auth | set `sessionStorage cf_user='acct_admin'` + `cf_auth='1'` |
| No build step | edit `index.html` → `node --check` the big `<script>` → preview-verify (if possible) → `git commit -F` (+Co-Authored-By) → push → confirm live by grepping the served page for a marker |
| GovWin sync deps | extractors (pdf-parse/mammoth/jszip/xlsx) live OUTSIDE OneDrive at `%LOCALAPPDATA%\atlas-govwin-sync`; the sync script loads them via the `GOVWIN_DEPS` env var |

**Standing hazards / environment quirks (read before touching prod / server.js):**
- **OneDrive flakiness.** Repo + `node_modules` are under OneDrive, which de-hydrates nested files — it broke `pdf-parse` locally (empty vendored dir), which is why the GovWin extractor deps live OUTSIDE OneDrive. The plain `Documents\CFResourcePlanner` path is a junction and can break; use the OneDrive path.
- **`server.js` is committed and active** (GovWin IQ API adapter + ingest/score endpoints, landed 2026-06-23). The old "never commit server.js" rule is **RETIRED**. Still stage files explicitly (never `git add -A`). The GovWin IQ API path stays dormant without `GOVWIN_*` creds.
- **Single-row JSONB, last-write-wins.** Prod data edits: `GET /api/data` (capture `X-Atlas-Updated-At`) → modify → `POST` with header `X-Atlas-Base-Updated-At: <stamp>`; **back up to `_backups/` first**. PROTECTED_ARRAYS guard 409s if a key array would shrink to 0. Hard-refresh before UI edits.
- **No `DATABASE_URL` in the shell** → can't run `server.js` locally. The Claude_Preview server (`npm start`, name `atlas` in `.claude/launch.json`) historically had the DB but the launch config is flaky. Server endpoints are verified at deploy on Railway (poll the served page / hit the endpoint).

## Major areas
1. **Contract Management** (`renderContractsView`) — standalone **BCLM** contract: OY staffing (`c.positions[]`) + a task tracker (`c.dashboard.tasks[]`). Self-contained (no `allM`/projects). Tabs below.
2. **GovWin Discovery** (Opportunities → 🛰 Gov Discovery) — capture pipeline: folder/CSV ingest, deterministic fit score, AI Bid/No-Bid scoring, No-Bid disposition, in-app doc viewer, stage kanban board. Backed by `server.js` + `gov_opportunities`/`gov_opp_docs` tables.
3. **Proposals** — per-section include checkboxes; custom-prompt section scoping; new "To Be Drafted" status.
4. **Strategy** — Break-even "Simulate Cuts" drives the Overview (`_stratCashCut`). Tabs: Overview · Break-even · Debt-Free Plan · Goals & KPIs · Revenue · **Team Expectations** (renamed from Team; the redundant Initiatives tab was removed). **Goals & KPIs is now DATA-BOUND** (`STRAT_METRICS` is a source registry; `_stratGoalEval` is a dispatcher over each source's `resolve(goal,model)`). Sources: cash-gate, annual-revenue, monthly-runrate, weighted-pipeline, initiative-revenue, **customers.arr**, **customers.health** (login bucket), **commercial.metric** (dept+metric), **tasks.completion** (BCLM pct/open/late), manual. Direction-aware status via `_goalDir` (`g.dir` override or source `dir`, supports "lower is better"); per-source formatting via `_goalFmt` (money/number/percent). Custom goal editor modal (`stratEditGoal`) = grouped source picker → conditional param fields (`_sgParamsHTML`) + a **live preview** + a collapsible **Linked items** section (opps/proposals/milestones checkboxes + a customer datalist chip-adder for accounts) stored on `g.links`; goals also carry `sourceParams`. The on-page Goals table dropped its Metric column and orders Target before Current.
5. **Commercial** (`renderCommercialView`) — NEW top-level section (gold nav tab after Contract Mgmt, admin-only by default). Home for the Scholarship & Grants commercial-side weekly status data. Data slice `D.commercial{submissions[],clientLedger[]}` on the single-row JSONB, fed by in-app weekly intake forms (not decks). **All six departments wired** (2026-06-29), now **v2** with a customer dimension: each submission has `customers[]` (per-account line-items; columns from `COMM_CUST_DEFS[dept]`) + structured `narrative{summary,highlights[],risks[],next[]}`. Customer combobox sources from all `project.subClients` (Awards 169 + Educian 118). Dashboards are **sub-tabbed** (Overview/Customers/Trends/Narrative via `commUI.dashSub`/`commDashSubTabs`) with a **week selector** (`commUI.dashWeek`) and **inline ✎ Edit** on every sub-tab (Save/Cancel; section-guarded `commFormCollect` so editing one section never wipes the others). Form is **draft-based** (`commUI.draft`+`commFormCollect`) with a dynamic customer-row editor. **Department dropdown** on Dashboard + Submit-weekly (`commUI.dept`, `commDeptBar`/`commSetDept`/`commFormDept`). Tabs: Dashboard, Submit weekly (snapshot vs flow groups per dept, week snaps to Friday, dept+week composite key edits in place), Schema. `commPersist()` delegates to ATLAS `save()`. `COMM_DEFS` per-dept metric registry: `kind` flow(sum/bars) vs snapshot(latest/line); `dir`+`band` → RAG; `lead:true` marks the trend-line metric; `money:true` → $ formatting. **`commDashHTML` is a router**: support=bespoke (median-reply line + throughput/SLA), sales/success/infra/marketing=`commDashGeneric` (KPI cards + lead-metric `commLine` trend; marketing adds conversion strip + skips trend since 1 week), impl=`commDashNarrative` (weekly note timeline). `commLine` generalized (RAG band optional, `commFmtMetric` labels). Seed source = 6 weekly status decks (`SW <date> 2026.pdf`); extracted with vendored pdf-parse (`%LOCALAPPDATA%\atlas-govwin-sync`). **CURRENT (2026-06-30): 7 departments** — support, sales, success, impl, **development** (added), infra, marketing (now a **5-week funnel series**, not a one-off). **35 submissions / 159 customer line-items** in prod; **79 line-items linked to the Customer registry** via `customerId` (account field type-aheads from `commCustNames()` = registry names; 🔗 on linked rows; rest are prospects/reselectable). **Dept-specific sub-tabs:** Sales **"Pipeline $"** (stage-weighted forecast — potential/weighted/at-risk, `commSalesPipeline`/`COMM_SALES_PROB`); Client Success **"Login activity"** (`commSuccessLogins` — buckets `D.customers` by login recency). Excluded from metrics: "clients lost YTD" + April's 30-day deadline window (inconsistent across decks). ⚠ **RE-SPLICE GOTCHA:** the Commercial module and the Customers module are adjacent in `index.html` before `function renderContractsView` — when re-splicing Commercial, END the splice at the Customers start comment (`/* … ATLAS · Customers — master client registry`), NOT at `renderContractsView`, or you delete the Customers module (happened in 655e8f8, fixed f99e541).

6. **Customers** (`renderCustomersView`) — master client registry `D.customers[]`. **No longer a main-nav tab — it's now a tab UNDER Commercial** (Commercial tabs are Dashboard · Submit weekly · **Customers**; the Schema tab was removed). Also reachable wherever `renderCustomersView()` is embedded. Master client registry (**337 records, 723 deadlines, ~$620K ARR**). Seeded by merging `LAST LOGINS CLIENT DATA.xlsx` (Active Clients + Latest Logins + Deadlines_2026, by Site URL) with project `subClients` (by name). Record fields: `name,url,type,region,version,siteStatus,latestLogin,daysSinceLogin,product,value(ARR),renewalDate,project,contact,notes,deadlines[],source`. Page = **filter-aware** KPI strip (cards recompute on the filtered rows) + search/region/**project**/type/status/source filters + sort + table; row→`ovCust`/`mdlCust` modal add/edit/delete (all fields + read-only deadlines); **"Sync from projects"** pulls new subClients. Edits gated `custCanEdit()`=admin/editor (viewers read-only); **Capture role can't see it**. All 118 **Educian Software** customers set `type='SIS'`. Built with vendored xlsx at `%LOCALAPPDATA%\atlas-govwin-sync`. Module sits right after the Commercial module in `index.html` (see the re-splice gotcha above).
7. **Access control & Valuation.** Roles `admin`/`owner`/`editor`/`viewer` + **`capture`** + **`exec`** (new). Capture sees ONLY Opportunities + Proposals + Recruiting. **`exec`** (Executive — for CEO/COO/CFO): `canProforma()` now returns `admin||exec`; `canSee('home'|'proforma'|'strat'|'valuation')` gate on `canProforma`; `canSee` returns true for exec on all other sections (sees everything read-only), but `can()` leaves exec non-editing and the Admin gear is admin-only. Additive. `ROLE_LABELS.exec='Executive'`. **Valuation** (`renderValuationView`, admin-only): backlog display-only, doesn't feed SOTP/EV/equity.

8. **Executive Home** (`renderHomeView`, viewMode `home`, 🏠 nav tab **first**) — NEW single shared exec dashboard for CEO/COO/CFO; **default landing** for anyone who `canSee('home')` (admin+exec, one-time `_homeLanded` redirect in `renderView`). Scoped CSS injected per-render (`_homeStyle`, `.exh-*` namespace); container capped at **1320px** (`max-width:1320px;margin:0 auto`). Structure: **North Star strip** (`_homeNorthStar` — pinned tiles: cash gate, projected trough, FY27 revenue, weighted pipeline — reads `_stratCash`), then a **pinned full-width Goals & KPIs block** (`_homeGoalsBlock`, NOT a widget), then three **customizable lanes**: `perf` Performance / `exec` Execution / `risk` Risk. Widgets live in `HOME_WIDGETS[]` (each `{key,lane,size,persona,title,sub,render()}`, `render()` try/caught so one bad widget can't blank the page). Layout persisted in **`D.execHome{order:[keys],hidden:[keys]}`** + one-time normalization flags (`revPairV1`, `oppPlacedV1`, `oppStagesPlacedV1`); **customize (remove ✕ / ▲▼ reorder / + add picker) is admin-only** (`homeToggleEdit`/`homeRemove`/`homeAdd`/`homeMove`, `_homeEnsure`). **Persona lens** (All/CEO/COO/CFO, `HOME_LENS`, client-only) dims non-matching widgets. Widgets: revtodate, revenue (trajectory), commercial, customers · tasks, milestones, staffing, owners, **opportunities**, **oppstages** (pipeline by CRM stage), proposals, **gov** (Gov Discovery pipeline), **govstages** · risks, runway, dates, datagaps, overalloc. **Fiscal year = federal Oct 1→Sep 30**: Revenue to date = monthly run-rate × months since **Oct 1** (`_homeFY26MonthsElapsed`); Revenue trajectory = 3-yr FY26–FY28 grouped bars — **Contracted** (`_homeFYAnnual` sums per-month `capProjRevAt` over the Oct→Sep window; FY26 is a STACKED bar = to-date + remainder) vs **Projected/target** (gold, from `_stratProjModel().rows`). Gov widgets read `/api/govops?limit=300` (server caps 300) into a home-only cache **`_homeGov`** (independent of the Gov Discovery module's `_govState`). **Goals & KPIs scorecard columns** = Goal · Owner · Target date · Target · Current · Progress · **Opps · Opp $ · Prop · Accts · Contacts · Miles · Tasks · Profit**, + a ▸ toggle that expands a detail row of linked item names. Linked items come from `_goalLinkData(g)` = manual `g.links{opportunities,proposals,accounts,milestones}` ∪ AUTO-derived from the goal's `linkedInitiativeIds` (opps via `opp.initiativeIds`, proposals via `opportunityId`, accounts via `customer.project` **and Prospect `kind:'account'` linked to the initiative**, contacts = Prospect `kind:'poc'` linked + account contact strings, milestones/tasks via `activity.projectId`). **Profit = Σ linked initiatives'/projects' (revenue−cost) ONLY** (opportunities were removed — they were double-counting `expectedValue`, some of it bad data).

9. **Navigation reorg (this session).** Main nav order is now: 🏠 Home · Proforma · Strategy · Valuation · Capacity Planning · **Resources** · Resource Planning · Projects · **Initiatives** · Contract Mgmt · Commercial · Opportunities · Proposals · Recruiting · COE Tasks · Project Tasks. Removed from main nav (folded elsewhere): **Customers** (now a tab under Commercial via `renderCustomersView`), **Overhead** (now a Proforma sub-tab after Cost Structure → `renderProjView(q,'overhead')`; **admin-only** since Proforma is), **Reports** (now a Proforma sub-tab → `renderReportsView`; admin-only), **Org Chart** (now a sub-tab under Resources → `renderOrgChartView`). `SECTIONS` entries for the removed nav buttons were left intact (nav apply guards `getElementById` with `if(el)`). **Proforma** also lost: the **Marines** tab + the **Strategic Role** card (unused; Contract Revenue card KEPT — it's the sole editor for `project.timeline` which drives the Capacity forecast), the **Notable Clients** card (Overview), the **Projected Cash Trajectory** card (Financials — dup of Strategy Debt-Free Plan), and the **Monthly Profit** KPI in the header bar. **Strategy** lost its redundant **Initiatives** sub-tab and renamed **Team → Team Expectations**.

## Contract page tabs (`contractTab`)
`Staff Planning` · `Portfolio Dashboard` · `Project Performance Summary` · `Resource Dashboard`

- **Staff Planning** (`_contractStaffHTML`) — positions grid; Option-Year selector at top. Filters `contractFilter{oy,company,group,status,hideTBD}`. OY2→OY3 delta highlighting (computed, `_ctBuildOyDelta`) + persisted `p.review{flag,note}` markup (green add / amber change / red review) used for chart reconciliation.
- **Portfolio Dashboard** (`_contractDashHTML`) — sub-tabs (`contractDashTab`): Overview (cards/flow/queue/roster + SPHERE block + Team workstream&load; cards derive from live task counts), Workstream Details & Resources (`_ctDashWorkstreamsHTML`), Tasks by Group (`_ctDashByGroupHTML`, multi-select group filter, WS/T-S toggle, inline-editable), Tasks by Sprint (`_ctDashBySprintHTML`), Reports (`_ctDashReportsHTML`, exec rollup + editable/AI objectives).
- **Project Performance Summary** (`_contractPPRHTML`) — exec PMR; month picker; throughput; SPHERE; top contributors by who completed.
- **Resource Dashboard** (`_contractResourceDashHTML`) — per-person; grouped Transformation/Support; surfaces SPHERE desc-assignees (first-name→full-name); "Completed by" view.

## Task data model (`c.dashboard.tasks[]`)
Fields: `id, taskId, name, bucket, ws (Transformation|Support), wsx (WS0–WS14), priority, progress, due, completedDate, completedBy, late, assignees[], labels[], desc, source (P|S|MSR), sprint`.
- **Source**: `P` = Planner (SAF/CNBB tracker), `S` = Spreadsheet (SPHERE), **`MSR` = Monthly Status Report** (purple badge). `_ctSrc`/`_ctSrcLabel`/`_ctSrcBadge`; editable in the task editor.
- **Counts (2026-06-25): 1305 tasks** = 777 P + 158 S + **370 MSR** (330 completed accomplishments + 40 Not-started planned activities; planned also carry label `planned`). MSR tasks bucketed by LOE/category; generic **"MSR Accomplishments" (37)** is a catch-all for bullets whose slide had no category header.
- Other dashboard keys: `resources[]`, `groupObjectives{}`, `completedFilter{years,buckets,ws}`, `wsClass{}`, `personWs{}`, `sphere{}`, seed flags.

## Staff Planning positions (`c.positions[]`) — OY staffing (separate from tasks)
Fields: `id, oy ('OY2'|'OY3'), resource, fte, company, lcat, role, workstreamGroup, status (Filled/TBH/TBD/Reserve), rateHr, annualCost, civilianLead, contractorLead, pwsRef, review{flag,note}`. Annual = `rateHr*fte*1860` (OY3 prorated convention). OY3 reconciled vs the latest org chart (2026-06-24/25) — review markup written to prod; a few confirms still pending (Cloud/ICAM names, TBH role labels, the "Liz" box).

## GovWin pipeline (server.js + index.html)
- **Tables**: `gov_opportunities` (id `govwin_<GovWinID>`; `score`=fit; `no_bid`/`no_bid_reason`; `bid_score`/`bid_band`/`bid_rationale`/`bid_dims`/`bid_risks`), `gov_opp_docs` (extracted RFP/RFI text by GovWinID — **text only; binaries stay local**).
- **Endpoints**: `POST /api/govwin/ingest`, `POST|GET /api/govwin/:id/docs`, `GET /api/govwin/:id/doctext`, `POST /api/govwin/:id/score`; plus existing `/api/govops` (list/patch/ingest), `/api/llm/complete`.
- **Sync tool**: `tools/govwin-sync.mjs` (+ `govwin-sync.cmd`, `setup-govwin-sync.ps1`, `GOVWIN_SYNC.md`). Reads `…\OneDrive…\Documents\Govwin\index.csv` + per-agency `<GovWinID> - <Title>/` doc folders (resolves folders by GovWinID prefix — the CSV `Folder` col is truncated), unzips + extracts PDF/DOCX/XLSX/PPTX text, pushes deltas. Run: `GOVWIN_DEPS="%LOCALAPPDATA%\atlas-govwin-sync" node tools/govwin-sync.mjs --base https://cfresourceplanner-production.up.railway.app [--dry-run|--manifest-only|--limit N]`.
- **Bid/No-Bid**: the **fit** score (deterministic `govScore` vs `D.companyProfile`) is SEPARATE from **bid_score** (AI 9-dim weighted rubric over the RFP docs + company/partner knowledge; default `claude-sonnet-4-5`). Gov **stages** (identified→…→won/lost) are a capture funnel SEPARATE from CRM `OPP_STAGES`; promotion (→ CRM Opp / → Start Proposal, or stage dropdown → Qualified/Proposal) bridges to the pipeline + a **"To Be Drafted"** proposal.
- **Status**: first sync DONE — **232 opps + ~681 docs + 57 MB text** in prod. **PENDING**: register the Mon/Thu scheduled task (`./tools/setup-govwin-sync.ps1`); bulk-score the 232 (⚖ Score unscored — real LLM cost); set `SAM_GOV_API_KEY` on Railway (still unset); optional `GOVWIN_*` creds for the live IQ API.

## Conventions
No build step. After editing `index.html`: extract the big `<script>` and `node --check` it; preview-verify if the preview server is up (`sessionStorage cf_user='acct_admin'`+`cf_auth='1'`); commit via `git commit -F` + Co-Authored-By trailer; push to `main`; confirm live by grepping the served page for a marker. Data-only changes go via the API with the base-stamp guard + a `_backups/` snapshot first.

## Prod data writes done (via API, backups in `_backups/`)
Earlier: name fixes; task merges (V2 completed, V3 SPHERE); desc/labels/completedBy backfills; group objectives; 13 Lead-Gen opps. This session: GovWin first sync (232 opps + docs); **OY3 copy of Kierra Byrd** (rate 147.52); **OY3 chart-reconciliation review markup** + 4 green TBH placeholders; **369 MSR items captured** → corrected to **330 completed + 40 planned** + 3 Program-Oversight boilerplate reverts; **bucket tidy** (28 remaps, 37→24 buckets). **2026-06-26 (data-only, additive):** duplicated **FAMS OY2 → "C1 FAMS OY3"** project (`id_1782404396651_25z2o`, POP 2026-09→2027-09, FY2027, $76,944/mo timeline shifted to OY3; 3 resources Melissa Jones/Hashim Kalla/Ajaz Ahmed Beigh re-allocated via allocSchedule; 8 activities copied, contract-end milestone shifted to 2027-09-24); added **118 current customers to Educian Software** as `subClients` (112 Kashmir Schools + 6 Higher Education from `Educian Billing Details.xlsx`, $42,854 ARR → $3,571/mo, `revenueFromSubs:true`, Expentor note preserved) — mirrors the Awards Mgmt Platform subClients pattern. Backups `prod_data_20260625_pre_famsoy3.json` / `prod_data_20260625_pre_educian.json`. All reversible via `source`/`labels`; always fetch fresh + base-stamp guard. **2026-06-29:** `D.commercial.submissions` — (1) 5 Support weeks (`…155828…`), (2) +21 rows for the other depts → 26 (`…163050…`), then (3) **v2 REPLACE** all 26 with enriched records (structured narrative + **104 customer line-items**) (`prod_data_20260629172030_pre_commercial_v2.json`). **2026-06-29 (later):** **v3 REPLACE** → **35 submissions / 159 line-items** (added Development 5 + Marketing series; `…pre_commercial_v3.json`). **Customers:** seeded `D.customers` = **337** (`…pre_customers.json`). **2026-06-30:** Educian Software customers → `type='SIS'` (118; `…pre_educian_sis.json`); **linked 79/159 commercial line-items to the registry** (`customerId`; `…pre_link.json`). **2026-07-01 (this session):** (1) fiscal-year tags — **C1FAMS (Leidos) OY3 → FY2027** and **Educian Software → FY2026** (were untagged; feed the Executive-Home revenue trajectory; backup `…_pre_fytag.json`). (2) **Phoenix 2 Launch `targetRevenue` −10000 → 0** (`id_1779988260967_2ut9i`; the negative target was polluting the Capacity revenue projection — initiatives roll into projected revenue when staffed; backup `…_pre_phoenix2.json`). All one-time API writes via GET→modify→POST w/ base stamp + `_backups/` snapshot.

## Open notes / TODO
- **Assign the `exec` role** to the CEO/COO/CFO accounts (Admin → Accounts) — the role exists but hasn't been assigned to anyone yet; they'd land on the Executive Home on login.
- **Bad opp data:** the opportunity **"UCMC DD577 or workforce Cert App"** has `expectedValue` $600K > its `$300K` potential. No longer affects the Goals Profit column (fixed), but it inflates the north-star/Opportunities weighted-pipeline and anything reading `expectedValue` — worth correcting in the Opportunities module.
- **Exec Home revenue trajectory** only reflects projects **tagged** by `fiscalYear`; FY28 currently shows only BCLM OY4. Tag future-year contracts (or add option-year projects) to fill FY27/FY28. Projected/target bars come from `_stratProjModel` (base × plan growth, default 25%/yr) — retarget to Strategy goals/Proforma if desired.
- **Goals "Contacts"** is derived from Prospect POCs linked to the initiative + account contact strings; many customers have an empty `contact`, so counts read low until Prospects/contacts are populated & linked.
- **Nav tidy:** `SECTIONS` still lists `customers`/`over`/`reports`/`orgchart` rows (their top-nav buttons were removed; the Section-Visibility matrix still shows them — harmless, guarded by `if(el)`). Could prune for cleanliness.
- **Commercial line-item linking:** 80/159 line-items are unlinked (sales prospects; dev internal components Phoenix/SupportAI/SendGrid/Signature-mgmt; ambiguous "Community Foundation of Eastern Shore" Scholarships-vs-Grants; Dell Young Leaders; PEO International; some United Way variants). User reselects via the editor datalist; can re-run `scratchpad/linker.mjs` (dry-run reports, `APPLY=1` writes) after reselection or to map a specific name to a chosen registry entity.
- **Customers registry is a point-in-time snapshot** — "Sync from projects" pulls in new project subClients, but there's no spreadsheet **re-import** flow yet (could add an upload that re-merges `LAST LOGINS CLIENT DATA.xlsx`-style exports by Site URL while preserving manual edits).
- **GovWin still pending** (unchanged): register the Mon/Thu scheduled task (`./tools/setup-govwin-sync.ps1`); bulk-score the 232 (⚖ real LLM cost); set `SAM_GOV_API_KEY` on Railway (still unset); optional `GOVWIN_*` creds for the live IQ API.
- Task views (Tasks by Group / by Sprint) now render ~1305 rows with inline `<select>`s — heavy. Use the **Completed-scope filter**; consider a **source filter (P/S/MSR)** and/or virtualization.
- The generic **"MSR Accomplishments" (37)** bucket could be keyword-categorized into proper LOEs on request.
- **OY3 reconciliation** has a few open confirms (Cloud/ICAM Norman/Perry vs MB/Cloud Architect; TBH role relabels; the grey "Liz" box). The amber/red items are comments only — apply for real on confirmation.
- SPHERE task assignees live in the task `desc` (first names), surfaced via first-name→full-name map; could map to full names everywhere on request.
- First-pass T/S classification of completed Planner tasks is heuristic (2024–25 archive skews Support); MSR `ws` is keyword-inferred — adjustable per-task.

## Change log (newest first)

### Session 2026-07-01 (Executive Home + rebrand + nav reorg)
- `86b3c9f` **Light top menu** — `.hdr` navy gradient → white; recolored all header contents (title/app-name→navy, subtitle/stats→muted, search/user-badge/bell→white+border, tab bar→no bg, tabs→slate with a navy active pill, warn stat #FBBF24→#B45309). Kept the gold bottom accent. White (not transparent) because the header is `position:sticky`.
- `e95dd52` **Rebrand Atlas → MissionInsights.ai** — guarded global replace of the standalone app name (title, login wordmark, header app-name, all in-app copy). NOT changed: `X-Atlas-*` headers, `buildAtlasComputed`/`D.atlasComputed`, "Atlassian" vendor.
- `9262bbe` **Exec Home: two Gov Discovery widgets** — `gov` (pipeline: count/est-value/open/AI-scored/no-bid) + `govstages` (capture funnel by `GOV_STAGES`). Load `/api/govops?limit=300` (server caps 300) into home cache `_homeGov` (independent of `_govState`); async re-render.
- `0b2c483` **Goals Profit fix** — Profit column = Σ linked initiatives'/projects' (revenue−cost) ONLY; removed the opportunity `expectedValue` term (was double-counting + bad data, e.g. an opp with expectedValue $600K > its $300K potential). (Prompted by "Workforce Cert. App profit seems off".)
- `64bb2f2` **Revenue trajectory FY26 stacked** — FY26 Contracted bar = revenue-to-date (monthly×elapsed) + remainder, stacked (dark+light), summing to the FY total; FY27/28 single bars.
- `c7a34e5` **FY starts Oct 1** — Revenue-to-date months count from Oct 1 (`_homeFY26MonthsElapsed`); `_homeFYAnnual` sums the Oct→Sep window; trajectory gained **Projected/target** gold bars per FY (from `_stratProjModel`).
- `792af7f` **Goals: Opp $ column** — total potential of linked opps, compact money (`_homeMoneyShort`).
- `d5b3277`/`3bfdfc9` **Exec Home: Opportunities + Pipeline-by-stage widgets** (Execution lane, before Proposals; `_homeEnsure` placement flags).
- `82d29d4` **Prospects → initiatives** — added `linkedInitiativeIds` to prospects + a checkbox picker in the Prospect editor; `_goalLinkData` derives goal Accounts (kind account) & Contacts (kind poc) from prospects linked to the goal's initiatives.
- `16d45f0` **Goals scorecard: linked-entity columns** — Opps/Proposals/Accounts/Contacts/Miles/Tasks/Profit + ▸ expandable detail; `_goalLinkData` (manual `g.links` ∪ auto-derived from initiatives); goal editor gained a "Linked items" section.
- `219cffa`/`72068f0`/`992d5ca`/`255abd4` **Goals & KPIs as a pinned full-width block** above the lanes (`_homeGoalsBlock`, out of the customizable grid), fit to the 1320px column.
- `329565c` **Exec Home tweaks** — dropped Corporate Debt north-star tile; show ALL goals; added Revenue-to-date widget (admin can enter actual → `D.proforma.financials.revenueYTD`); Commercial-health widget dated by week.
- `fd31e2f`/`0828c98`/`2278400` **Revenue widgets** — FY26 monthly, then 3-year FY26–FY28 annual, then time-phased annual.
- `aa8001d` **Strategy Goals data-binding first slice** — `STRAT_METRICS` source registry + `_stratGoalEval` dispatcher + 4 new sources (customers.arr/health, commercial.metric, tasks.completion) + `_goalDir`/`_goalFmt` + custom goal editor modal w/ live preview.
- `e93828d` Goals table: dropped Metric column, Target before Current.
- `80a3877` **Executive Home shipped** — new `home` view + 🏠 nav + **`exec` role**; North Star + 3 lanes + customizable widgets (`D.execHome`), persona lens, admin-only customize. `8bcddbe` made Home the **default landing** for admin+exec.
- `d5d8d1a` **Proforma: removed Marines tab + Strategic Role card** (unused; Contract Revenue KEPT — sole editor of `project.timeline` that drives the Capacity forecast).
- `6b76309` Proforma: removed the Monthly Profit KPI card.
- `0e91202`/`4465c61` **Nav reorg** — Customers→under Commercial, Overhead & Reports→Proforma sub-tabs (admin-only), Org Chart→under Resources; removed those top-nav buttons; dropped Proforma Notable-Clients + Projected-Cash-Trajectory cards; reordered nav (Resources before Resource Planning, Initiatives after Projects).
- `4e2b0e0` Strategy: removed redundant Initiatives tab; Team → **Team Expectations**.
- `c21a9e8`/`fe7c43d`(prior) Commercial: **Customers** tab replaced **Schema**; Org Chart sub-tab under Resources.
- `3ad0bd5` Strategy Revenue: Team-member column; Goals & KPIs rendered as a columnar table.

### Prior sessions
- `9ffe503` **Commercial line-items linked to the Customer registry — LIVE.** Each `customers[]` line-item now carries `customerId` → `D.customers`. Account field is a type-ahead from the registry (`commCustNames`); on save `customerId = commResolveCustomerId(account)` (exact case-insensitive name) `|| hidden-field prior link` `|| ''`(prospect). New helpers `commCustNames`/`commCustById`/`commResolveCustomerId`; hidden `cf_cust_i_cid` per row preserves links across re-saves (so we DON'T canonicalize/rename accounts). Linked rows show 🔗 (editor + read-only customers table, with "N linked" count). **Auto-linked existing data (one-time write, conservative exact-normalized + unique-contains match; backup `…_pre_link.json`): 79/159 line-items** — support 9/11, success 33/58, impl 14/20, infra 15/24, development 3/13, sales 5/33, marketing 0 (no cust rows). Unmatched left as prospects for reselection (sales prospects; dev internal components Phoenix/SupportAI/SendGrid/Signature mgmt; ambiguous "Community Foundation of Eastern Shore" Scholarships-vs-Grants, Dell Young Leaders, PEO, etc.). Reselect = pick the registry name in the editor datalist. **Splice END anchor used = Customers module start comment (per the earlier gotcha).**
- `fe7c43d` **Customers: Project filter dropdown + filter-aware KPI cards + Educian→SIS — LIVE.** (1) Added a **Project** filter dropdown (`custUI.project`, `custDistinct('project')`) next to region/type/status/source. (2) KPI cards (Customers, Active sites, Scholarship, Grants, Engaged, Stale, Total ARR, Open deadlines) now compute from the **filtered `rows`** not the full list, so they update with the filters (header still shows "N total · M shown"). (3) **Data:** set `type='SIS'` for all **118 Educian Software** customers (were blank) — one-time API write, backup `prod_data_20260630150052_pre_educian_sis.json`. Type distribution now Scholarship 116 / SIS 118 / Grants 66 / blank 36. node --check + vm verified.
- `655e8f8`→`f99e541` **Client Success: "Login activity" sub-tab — LIVE.** New Client-Success-only dashboard sub-tab (`commDashSubTabs` adds `logins` for `success`; routed in `commDashHTML`→`commSuccessLogins()`, week-picker hidden like trends). Reads **`D.customers`** (the Customers registry) and buckets by login recency — Engaged ≤7d / Active 8–30d / At risk 31–90d / Dormant >90d — with a KPI strip (sites-with-data, need-attention=at-risk+dormant), recency distribution bars, and a full table sorted most-stale-first. Read-only over existing data, no data write. **GOTCHA fixed in `f99e541`:** the commercial re-splice end anchor is `function renderContractsView`, but the **Customers module sits between the Commercial module and renderContractsView**, so the splice deleted `renderCustomersView`/`cust*` (655e8f8). Restored by re-inserting `customers_module.js`. **Future: when re-splicing the Commercial module, END the splice at the Customers module's start comment (`/* ===… ATLAS · Customers`), NOT at renderContractsView.**
- `1d9ab7b` **Customers page — master client registry — LIVE.** New top-level **Customers** section (`renderCustomersView`, nav `tabCustomers`, SECTIONS `customers`, dispatch, `canSee` general → admin/editor/viewer yes, Capture no). Single editable `D.customers[]` registry. **Seeded 337 customers / 723 deadlines / $620K ARR** by merging the `LAST LOGINS CLIENT DATA.xlsx` (3 sheets — Active Clients + Latest Logins + Deadlines_2026, keyed by Site URL) with every project's `subClients` (by normalized name: 142 matched/enriched, 145 project-only added). Fields: name, url, type, region, version, siteStatus, latestLogin, daysSinceLogin, product, value(ARR), renewalDate, project, contact, notes, deadlines[], source. Page = KPI strip + search/region/type/status/source filters + sort + table; row→modal (`ovCust`) add/edit/delete (all fields + read-only deadline list); **Sync from projects** pulls new subClients. Edits gated `custCanEdit`=admin/editor (viewers read-only). Built with vendored xlsx at `%LOCALAPPDATA%\atlas-govwin-sync`. Backup `prod_data_20260629205026_pre_customers.json`. node --check + vm (render 337, filters, modal, sync, canSee/role).
- `166ee94` **Access control: new "Capture" role — LIVE.** Assignable role (ROLE_LABELS `capture:'Capture'`) with full-edit access to ONLY Proposals + Recruiting + Opportunities, no other page. `canSee` → `if(ME.role==='capture')return k==='prop'||k==='recruit'||k==='opp';` (page-level security boundary). `canProposal()`/`canRecruit()` += capture. `can()` → `if(r==='capture')r='editor'` so opp editing (gated by `can('globals')`) + all in-page edit actions work; page reach still bounded by canSee. `prospectAddTask` (BD tasks under Opportunities) += capture. Purely additive — admin/editor/viewer/owner unchanged (vm-verified all 5 roles: capture sees exactly opp/prop/recruit, canProposal/canRecruit/globals true, accounts false). Capture is NOT in the Section-Visibility matrix (fixed `roles=['owner','editor','viewer']`) by design. Admin assigns it per-account in Accounts. No data write.
- `993a6ab` **Commercial: Development department + Marketing as a multi-week series + narrative backfill — LIVE.** Audit found gaps: the decks' **Development/Software Updates** sections had no home, and the **email-campaign funnel is on every deck's Welcome slide** (not just May 1). Added **`development`** dept (narrative:true, `COMM_CUST_DEFS.development`=status/note; generic overview handles no-scalar-metrics depts → item counts by status). Removed Marketing `single` flag → it's now a 5-week series (Trends tab + lead trend). **Re-seeded v3 (REPLACE → 35 submissions, 159 customer line-items):** support/sales/success/impl/infra 5 each + **development 5** (May1/May22/Jun5/Jun19 Software Updates + Jun26 domain-expansion/signature-mgmt from the Welcome slide) + **marketing 5** (Apr24/May1/May22/Jun5 email-campaign funnel + Jun19 domain-hygiene). Backfilled narratives across all depts (fuller highlights/risks/next + more per-customer notes — e.g. success Apr24 now 12 client rows, sales Apr24/May22 activities from the leads slides). Backup `prod_data_20260629191937_pre_commercial_v3.json`. node --check + vm (7 depts × sub-tabs, no throws). NOTE still excluded: "clients lost YTD" (inconsistent) and April's 30-day deadline window.
- `35f0902` **Valuation: contracted-backlog months bounded by the contract's own term — LIVE.** Bug: `_valProjMonthsLeft` computed `end−now+1` ignoring `start`, so a future option-year term was valued as a cumulative multi-year span (BCLM OY3 showed 16 mo/$4.62M, OY4 28 mo/$8.09M). Fix (in `_valProjMonthsLeft`, index.html ~8648): cap months at the term's own length `end−start` when both dates present (added `_valMonthIdx` helper). General — no hardcoded amounts/names; in-progress terms (remaining<term) and no-date projects (default 36) unchanged. **Root cause was the calculation, not the date data** (start/end were correct 12-month terms). **Backlog is DISPLAY-ONLY** (Segments&revenue contract-terms table + segment "Contracted backlog" column); it does NOT feed SOTP/EV/equity → headline valuation unchanged. Verified vs live data w/ the real extracted fns: BCLM OY3 & OY4 → **$3,466,536** each; C1FAMS OY3 16→12; Awards/Educian 37→36 (term cap, minor); Gov segment backlog **$15.34M → $9.19M** (Δ −$6.14M), awards −$49,057, educian −$1,786.
- `e866d11` **Commercial/Sales: pipeline revenue + loss forecast ("Pipeline $" sub-tab) — LIVE.** Sales-only dashboard sub-tab (`commDashSubTabs` inserts `pipeline` after overview; routed in `commDashHTML`→`commSalesPipeline`). Stage win-probabilities `COMM_SALES_PROB` (Qual 20 / VP-Demo 40 / Pricing-Proposal 60 / Verbal 90 / Won 100 / Lost 0); open stages `COMM_SALES_OPEN`. KPI strip: Potential revenue (open pipeline), Weighted/expected (Σ value×prob), At risk (potential−weighted), Closed-won, Closed-lost. Per-opp table (account/stage/win%/potential/weighted + totals footer) + by-stage value bars. `commSalesEconomics(cur)`: per-deal `value` from the editable customer field; **where a deal value is blank, the reported `pipelineValue` is apportioned across open opps by stage weight (marked "est")** so it reconciles to the real total (Jun26 $92,526→weighted $51,061; Jun19 $70,495); entering a value on Customers overrides. `commMoney` helper. UI-only, no data change.
- `84936c8` **Commercial: inline editing throughout the dashboards + week selector — LIVE.** Seed data is now editable in context, not only via Submit-weekly. **Week selector** per dashboard (`commUI.dashWeek`, `commCurFor`/`commWeekPicker`) — Overview/Customers/Narrative show & edit any week (Trends stays all-weeks). **Per-sub-tab ✎ Edit + Save/Cancel** reusing the form draft: Overview→metric fields, Customers→inline row editor (add/remove, account combobox), Narrative→summary/highlights/risks/next. KEY: `commFormCollect` is **section-guarded** — only rebuilds customers when `#comm_cust_rows` is present, only touches fields/narrative when their inputs render, so a partial editor never wipes sibling sections (verified in vm: edit-metrics keeps customers+narrative, etc.). New handlers `commSetDashWeek`/`commDashEdit`/`commDashSaveEdit`/`commDashCancelEdit`; overview/customers/narrative renderers take the selected week (`commPrev`/`commFieldRow`/`commEditBtn`/`commEditActions`). UI-only, no data change.
- `1713bee` **Commercial v2: customer dimension + structured narrative + sub-tabbed dashboards — LIVE.** Big sophistication pass, all six depts. **Data model:** each submission now has `customers[]` (per-account line-items) + structured `narrative{summary,highlights[],risks[],next[]}` (back-compat: old `note`→summary). **`COMM_CUST_DEFS`** per-dept customer columns (sales stage/value/close; support open/issue/escalatedTo; success health/meetings/renewal; impl phase/status/goLive; infra item/status; +note each). **Customer combobox** = `<datalist>` from `commCustomerList()` (unique `account` across ALL `project.subClients` → Awards Mgmt 169 + Educian 118 ≈ 287) + free-type for prospects. **Dashboards sub-tabbed** (`commUI.dashSub`): Overview / Customers / Trends / Narrative (`commDashSubTabs` decides which apply — impl drops Trends, marketing only Overview+Narrative). Overview routes: support=bespoke, impl=derived status counts, others=generic KPI+lead-trend (+marketing conversion strip). Customers tab=RAG-chip table; Trends=small-multiples (`commLine` per metric); Narrative=structured blocks newest-first + per-account notes. **Expanded `COMM_DEFS`:** sales +closedWon/activeStages; success +wellnessResponses; infra +deadlinesPassed/mtdActiveApps; marketing +loopClosed/archivedBounced/notInterested/orgsIdentified/totalContacts. **Form is draft-based** (`commUI.draft` + `commFormCollect`) so add/remove customer row + dept-switch never lose input; dynamic customer-row editor; structured-narrative textareas. New scoped CSS `.comm-custrow/.comm-nlabel/.comm-nh/.comm-nlist`. Module rewritten + spliced (CRLF-preserved). Verified: `node --check` + Node vm across all depts×sub-tabs (no throws) + edit-mode forms. **Re-seeded** prod (REPLACE all 26 submissions with enriched: fields + structured narrative + **104 customer line-items**) from the 6 decks; backup `prod_data_20260629172030_pre_commercial_v2.json`, base-stamp guard, POST 200.
- `9c05b30` **Commercial: wired the remaining departments + Dashboard department dropdown — LIVE.** Extended the module from Support-only to all six deck departments. `COMM_DEPTS` all `ready`; added **`infra`** (Infrastructure) and kept `marketing` as a single-snapshot. `COMM_DEFS` registries: **sales** (totalOpps, pipelineValue $ lead), **success** (meetingsMonth lead, meetingsNext, nps band 70/50, csatResponses), **infra** (activeSites lead, deletionsQueue, deadlinesUpcoming), **marketing** (emailsSent lead, researchedLeads, followupsScheduled, interested, meetingsBooked, orgsIdentified, totalContacts); **impl** has none (narrative-only). `commDashHTML`→router: support bespoke unchanged; `commDashGeneric` (KPI cards + lead `commLine` trend, money-aware; marketing adds `commMktConversions` strip + skips the 1-week trend); `commDashNarrative` for impl. `commLine` band-optional + `commFmtMetric` labels (so the support line now reads 20m→39m); `commKpi` money-aware. Form/ledger generalized per dept (`commFormDept` switches fields; impl shows narrative-only; ledger cols from `COMM_DEFS` or a narrative excerpt). Dropdown via `commDeptBar`/`commSetDept`. Verified: `node --check` + all six dashboards/forms in a Node vm. **Seeded** sales 5 / success 5 / infra 5 / impl 5 / marketing 1 (+21 → 26 total) via one-time API write (backup `prod_data_20260629163050_pre_commercial_depts.json`, base-stamp guard, POST 200). Source = the 6 `SW <date> 2026.pdf` decks.
- `2d362e1` **Commercial module (Customer Support dashboard) — NEW top-level section, LIVE.** Parallel to Contract Management; one file touched (`index.html`). CSS pasted into `<style>` (fully namespaced `.comm-root`/`--comm-*`); JS module pasted before `renderContractsView` (`COMM_DEPTS`/`COMM_DEFS`/`commUI`/`D.commercial`/`renderCommercialView`/handlers/`commPersist`/`commLoadSeed`). Nav: gold `💼 Commercial` button + `SECTIONS` entry (`{k:'commercial',tab:'tabCommercial'}` → auto-adds the Section-Visibility matrix row). Dispatch: `viewMode==='commercial'→renderCommercialView()`. **Access:** `authMigrate` seeds explicit `commercial:false` for owner/editor/viewer so it's admin-only until granted (matrix renders unchecked, consistent). **Persistence:** `commPersist()` was changed from the drop's reference fetch to delegate to ATLAS's canonical `save()` (localStorage mirror + debounced `serverSave` w/ `X-Atlas-Base-Updated-At` stamp + 3-way-merge/`PROTECTED_ARRAYS` 409 handling) — better than the reference's clobber-alert; `submittedBy` uses `ME.email`. No `server.js` change (existing `/api/data` surface). Verified: `node --check` on extracted `<script>`; render logic in a Node `vm` against the seed (reply line 20→21→24→29→39, latest red, Friday snap, snapshot/flow grouping, seed idempotent). Seeded 5 real CS weeks via one-time API write (backup `_backups/prod_data_20260629…_pre_commercial.json`, base-stamp guard, POST 200) — confirmed 5 submissions live. **Next drops (not built):** Sales (reuse gov Reports metric/dim toggle), Client Success (`clientLedger` as single source for churn/active-sites).
- `70afe65`→`7c4471c` (merged via `778b385`) **Company Valuation page (Admin-only) merged to `main` + extended — NOW LIVE** (was on `feature/valuation-model`, "not yet live" per earlier notes). Sum-of-the-parts + DCF, 3 scenarios, 4 segments; SOTP/DCF/EV explained on the tab; Assumptions show dollar amounts + guidance notes; scenario cards show labeled Blended EV + Equity value. Projects gained **contract term as a first-class field** (editable Start/End dates → months remaining + contracted backlog; default 36-month term when no dates entered). **FY26–FY29 projected revenue** sourced from **Strategy → Revenue goals** (not `project.targetRevenue`) + an initiatives panel; initiative target revenue feeds segment value (SOTP/EV). Deliberately merged + verified live for idris (admin) in a separate valuation session (2026-06-26); see memory `project-atlas-valuation-model`. Method: SOTP (per-segment revenue multiples) + 5-yr DCF, blended 60/40, EV − net debt (~$2.99M) → equity across Worst/Conservative/Best. Default-multiple result: equity $1.65M / $4.28M / $8.49M.
- `20422ba` Gov Discovery **Reports** made configurable/sophisticated: **metric toggle** (Count vs Pipeline value) reskins every breakdown; **"Break down by"** dropdown with 11 dimensions (stage, agency, sub-agency, NAICS, set-aside, lifecycle, bid band, fit band, value band, new-vs-recompete, no-bid status) via reusable `GOV_RPT_DIMS`/`_govRptSeries`; 8 KPI cards (count+distinct agencies, pipeline value, avg/median value, scored %, avg fit, no-bid %, recompetes, won/lost); fit-score distribution histogram + metric-aware by-value-band/by-stage/by-bid-band charts; Top-12 opportunities-by-value table. State `_govRpt{metric,dim}` + `govRptSet`; toggles recompute client-side from the loaded list (no refetch); still respects the filter bar.
- `565f227` Gov Discovery: **Agency filter → dropdown** ("All agencies" + union of full agency set captured into `_govState.agencies` on an unfiltered load + current-list agencies + active selection, so options never collapse) and **Reports tab** added — List/Board toggle replaced by three view buttons (List/Board/Reports); `_govReportsHTML` renders pipeline analytics over the current (filtered) list. Display-only, no server changes.
- `403071b` Contract Staff Planning: **Alternate (backup resource) per position** + **OY2→OY3 role-change indicator**. (1) New **Alternate** column in the positions grid (after Resource) + edit-form field, stored as `p.alternate`, inline-editable via `contractUpdatePos` — a backup resource per seat (display/data only; doesn't feed cost or the delta). (2) `_ctBuildOyDelta` now also detects **role** changes per resource between OY2/OY3 (case-insensitive compare) — flags the amber change-highlight with a `role: old→new` note merged with any FTE/company change; legend now reads "FTE / company / role changed". Empty-row colspan bumped 11→12.
- `4ded3c5` PPR: **Sources filter** (P/S/MSR) in "Accomplishments by category" — `_ctPPRSrc`/`ctPPRSetSrc`; cascading dropdown (month → source → category) narrows the month-scoped completed set before grouping, so category options/counts and the render re-scope to the selected source; subtitle + empty-state mention the active source
- `7752dc9` Contract Staff Planning: computed **Funding gap** KPI (`Contract floor − Annual cost`) beside Contract floor — green when ≥0, red when cost exceeds the floor; shows only when a floor is set; uses the same filtered annual cost as the Annual cost KPI
- `db100ea` Access Control: made **Section Visibility authoritative**. (1) `renderContractsView` gated on `canProforma()` (admin-only) so a granted Contracts section still showed "restricted to Admin" → now gates on `canSee('contracts')`. (2) `canSee('prop')`/`canSee('recruit')` returned `canProposal()`/`canRecruit()` and ignored `D.sectionVis` entirely (matrix toggle was a no-op) → `canSee` now treats an explicit grant/revoke as authoritative; an UNSET cell falls back to the module capability (preserves defaults). Edit actions stay capability-gated (read-only when a tab is granted to a non-capable role); Proforma/Strategy stay admin-only by design. Note: every non-admin account is role `editor` — there are NO `viewer`-role accounts (so role-level toggles hit all editors; set an account's role to `viewer` for read-only). Contract Mgmt has no separate edit gate yet (granted = full edit).
- `1f597f0` Proposals: **SBIR/STTR proposal templates** — two agency-agnostic templates seeded into `D.proposalTemplates` alongside `tpl_cf_technical`: **`tpl_sbir_phase1`** (Phase I, 15 sections) and **`tpl_sbir_phase2`** (Phase II / Direct-to-Phase-II, 16 sections, modeled on the real InsightForge-AI SF254-D1201 D2P2). Each AI section has an SBIR/STTR-tuned prompt (phase-feasibility framing, Phase III nexus, ITAR/foreign-national disclosure, use-only-real-data guardrails) that adapts to the loaded topic context. Purely additive: `_sbirPhase1Template()`/`_sbirPhase2Template()` + idempotent seed block (`sbirTemplatesSeeded` flag). No server changes — plugs into existing New-proposal → template pick → Sections materialize → Draft All Sections / include checkboxes / custom-prompt scoping / assembly. Best output when the topic doc is attached (RFP/topic upload) and Knowledge Bank/Company Profile carry real award + team data.
- `18d107b` PPR: **category filter** in "Accomplishments by category" (Project Performance Summary) — `_ctPPRCat` state + `ctPPRSetCat` setter mirror the month-filter pattern; "All categories (N)" dropdown beside the month picker narrows both render modes; orthogonal to the month filter (they combine); self-clears if the category isn't present after a month change
- `c351413` Contract tasks: added **MSR** task source (purple badge, editor + _ctSrc/_ctSrcBadge). Then captured MSR items from the 9 monthly decks into BCLM tasks — all source=MSR, label MSR, bucket=LOE/category, ws keyword-inferred. Final state (data-only, prod writes, backups in _backups/): **BCLM tasks 935→1305; MSR=370 → 330 Completed accomplishments + 40 Not-started planned activities** (planned also carry label 'planned'). Note: planned bullets sit above the slide's "Planned Activities" footer so the first accomplishment pass mis-swept 42 as Completed → corrected to Not-started; 3 recurring Program-Oversight boilerplate lines reverted to Completed. Reversible via source=MSR / labels. Bucket tidy: remapped 28 stray task-sentence/one-off buckets to proper LOE/categories + merged SPHERE near-dupes (37→24 distinct); generic "MSR Accomplishments" (37) kept as catch-all
- `163b59b` Gov Discovery: pipeline board (kanban by gov stage) — List/Board toggle, drag-to-change-stage (drag→Qualified/Proposal still prompts promote via govStageChange), optimistic govSetStage. Gov stages are a capture funnel SEPARATE from the CRM OPP_STAGES pipeline; promotion bridges them
- `230513a` Staff Planning: "Hide TBD" filter toggle — excludes status=TBD positions (`contractFilter.hideTBD`), wired into filter/Clear/anyFilter
- `3dc47eb` Staff Planning: render persisted `p.review` markup (chart-reconciliation comments/colors) — add=green/change=amber/review=red, priority over computed delta. Used to mark OY3 vs latest org chart (annotations + 4 green TBH placeholders written to prod, backup in _backups/)
- `dce8a8f` Contract Staff Planning: OY2→OY3 delta highlighting — per-resource compare (computed, no data write) colors position rows green=NEW in OY3 / amber=FTE-or-company changed / red=removed, + legend; company designation suffixes normalized out (`_ctBuildOyDelta`)
- `748a00f` Gov Discovery: stage dropdown promotes — Qualified→create CRM opp, Proposal→start proposal (confirm + no-dup guard via govOppId; decline = label only); added "To Be Drafted" PROP_STATUS, gov-pipeline proposals land there (`govStageChange`)
- `5a187aa` Gov Discovery: in-app RFP/RFI document viewer — `📎 Docs (N)` per opp opens a list + reads extracted text (`GET /api/govwin/:id/doctext`; `doc_count` in /api/govops). Originals stay local (text-only in `gov_opp_docs`)
- `5fe48a4` Bid scoring: max_tokens 3000 (fix JSON truncation) + tolerant parse
- `32f4654` Gov Discovery: AI Bid/No-Bid scoring Phase 2 — POST /api/govwin/:id/score (opp + RFP docs + Company Profile → 9-dim rubric → band/score/rationale/risks); per-opp Score button + band chip + assessment panel + "Score unscored" bulk + band filter
- `04bc61d` Gov Discovery: No Bid disposition (no_bid column + per-opp toggle/badge/filter `govSetNoBid`); GovWin sync robustness (strip NUL/C0, non-fatal per-opp, incremental state)
- `618be55` GovWin ingest Hybrid Phase 1: gov_opp_docs + /api/govwin/ingest + /docs, Gov Discovery "Import GovWin index.csv", tools/govwin-sync.mjs (+cmd/ps1/md); commits held GovWin IQ API adapter. First sync: 232 opps + 681 docs + 57MB text
- `03ebf46` Proposals: editable custom prompt on Sections tab + "Select sections from prompt" — parses section refs (1.2/1.4, "Section 3", or titles) → sets include flags, scoping both AI gen and assembled output (`_propSectionsMatchingPrompt`/`pdScopeSectionsFromPrompt`)
- `4187a84` Proposals: per-section include checkboxes (default all on) — gate both "Draft All Sections" AI gen and the assembled/printed proposal (`_secIncluded`/`pdToggleSectionInclude`)
- `0616b74` Resource Dashboard: group resources by Transformation/Support + surface SPHERE desc-assignees (first-name→full-name map); Strategy: Break-even cuts now drive the whole Overview (`_stratCashCut`)
- `95eb13a` Tasks by Group: multi-select groups; Sprint tag; new Tasks by Sprint tab; live Overview counts
- `495c1eb` Task Source (P/S) + import 158 SPHERE plan tasks
- `e242e9f` Tasks by Group: inline-editable table
- `d2d052a` PPR top contributors ranked by who completed
- `a4495e6` Resource Dashboard ordered by completed count
- `901bcd6` Resource Dashboard completed view = tasks completed by selected resource
- `b7964ed` OY selector to top; SPHERE sections above Team; Completed-by columns
- `6924e8b` Editable + AI task-group objectives
- `b1e7da9` Show Completed By on completed rows
- `7716669` Tasks by Group grouped by SPHERE workstream (WS0–WS14)
- `4ca6f92` Select all/Deselect all on checkbox lists + Completed By
- `da1238a` Added Tasks by Group sub-tab
- `b603862` PPR month picker + by-category; resource hover details
- `852e426` Completed-task scope filter (year/group/workstream)
- `88379ff` Completed tasks + Resource Dashboard + Project Performance Summary
- `822215e` Fix Donald Vanmeter spelling in seed
- `d9c42c1` Short section descriptions
- `22ce10c` Legible task-group headers + Reports tab
- `0a8c4fc` Workstream Details + self-contained resources
- `1be5134` Initial BCLM portfolio dashboard in Contract Management
